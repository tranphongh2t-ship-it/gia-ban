{
  "name": "gia-ban-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate": "wrangler d1 migrations apply gia-ban-db --remote",
    "migrate:local": "wrangler d1 migrations apply gia-ban-db --local"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260702.1",
    "@types/better-sqlite3": "^7.6.13",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2",
    "wrangler": "^3.0.0"
  }
}
